import colors from '../settings/colors';

const styleSheet = {
  iconMenu: {
    position: 'relative'
  },
  clickAway: {
    pointerEvents: 'all',
    position: 'fixed',
    width: '100%',
    height: '100%',
    top: 0,
    left: 0,
    opacity: 0,
    zIndex: 1
  }
};

export default styleSheet;
