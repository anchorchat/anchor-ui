import colors from './colors';

const styleSheet = {
  profileCard: {
    padding: '20px',
    fontSize: '16px',
    backgroundColor: colors.green,
    color: colors.white
  }
};

export default styleSheet;
