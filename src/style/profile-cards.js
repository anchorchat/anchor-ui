import colors from './colors';

const styleSheet = {
  profileCard: {
    padding: '15px',
    fontSize: '18px',
    lineHeight: '18px',
    backgroundColor: colors.green,
    color: colors.white
  }
};

export default styleSheet;
