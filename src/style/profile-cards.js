import { colors } from '../settings';

const styleSheet = {
  profileCard: {
    backgroundColor: colors.theme,
    color: colors.white,
    fontSize: '18px',
    lineHeight: '18px',
    padding: '15px'
  },
  avatar: {
    lineHeight: '40px',
    overflow: 'auto'
  }
};

export default styleSheet;
