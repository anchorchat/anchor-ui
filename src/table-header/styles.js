import colors from '../settings/colors';

export default {
  root: {
    backgroundColor: colors.theme,
    color: colors.white,
    fontWeight: 'normal',
    fontSize: '16px',
    transition: 'background .3s ease-in-out'
  }
};
